# Copy to .env and set a long random secret in production.
# Generate example: openssl rand -hex 32

JWT_SECRET=change-this-to-a-long-random-string

# production | development (affects secure cookie flag)
# NODE_ENV=production

# Set to 1 for verbose server / WebSocket logs (default is quiet)
# CHATTIST_DEBUG=1
